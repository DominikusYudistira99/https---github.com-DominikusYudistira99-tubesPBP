package model

type User struct {
    Username string
    Password string
}

func (u *User) Mengisi() {
    // Method untuk mengisi data user
}

func (u *User) Melihat() {
    // Method untuk melihat data user
}

func (u *User) Mengubah() {
    // Method untuk mengubah data user
}
